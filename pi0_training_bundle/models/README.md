Put local model snapshots here.

Recommended layout:

```text
lerobot_pi0/
  config.json
  model.safetensors
  policy_preprocessor.json
  policy_postprocessor.json
```

Alternatively, copy Hugging Face cache directories into `../hf_cache/hub/`.
